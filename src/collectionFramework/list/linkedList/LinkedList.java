package collectionFramework.list.linkedList;

/*
LinkedList는 데이터(item)와 주소로 이루어진 클래스를 만들어 서로 연결하는 방식
데이터와 주소로 이루어진 클래스를 Node(노드)라고 하는데
각 노드는 이전의 노드와 다음 노드를 연결하는 방식(=이중 연결 리스트 = 객체끼리 연결한 방식)
요소를 검색해야 할 경우 처음 노드부터 찾으려는 노드가 나올 때까지 연결된 노드들을 모두
방문해야한다는 점에서 성능이 떨어지지만(검색 x)
해당 노드를 삽입, 삭제해야할 경우 해당 노드의 링크를 끊거나 연결만 해주면 되기 때문에
삽입, 삭제에서는 매우 좋은 효율을 보인다.(삽입, 삭제 0)
*/
public class LinkedList {
}
